class AnnotationService:
    """Creates and manages annotation overlays and markup operations."""

    def add_text_annotation(self, page_index: int, text: str) -> None:
        _ = (page_index, text)
